# ЗАДАЧА ДЛЯ ФРОНТЕНДЕРА:
Пока ML и DE пишут модули - работает с моковыми данными. 

## ЗАДАЧА 1: обвязка пайплайна в app.py
Вкладка "Подбор". Логика на кнопке "Подобрать":

```python
subtasks = decompose(query)
for st in subtasks:
    candidates = retrieve(st.topic, k=20)
    top5 = rerank(st, candidates)
    show_cards(st, top5)
```

Пока модули не готовы - используй заглушки, которые возвращают моковые объекты. Как только DE и ML начнут мерджить реальные - заменишь импорты.

## ЗАДАЧА 2: карточка кандидата
По шаблону из мокапа, который я описывал: аватар с инициалами, ФИО, подразделение, скор в цветной плашке (0.7+ зелёная, 0.5-0.7 жёлтая, ниже - серая), 2–3 причины с точками-маркерами, разворачиваемый блок "Черновик письма" с кнопкой "Копировать".
Драфт письма - вызов draft_email(query, profile) по клику на "Развернуть", не заранее (иначе на каждый запрос будет 5 вызовов LLM - долго и дорого).

Аватар с инициалами, ФИО, подразделение, скор в цветной плашке (0.7+ зелёная, 0.5-0.7 жёлтая, ниже - серая), 2–3 причины с точками-маркерами, разворачиваемый блок "Черновик письма" с кнопкой "Копировать".
Драфт письма - вызов draft_email(query, profile) по клику на "Развернуть", не заранее (иначе на каждый запрос будет 5 вызовов LLM - долго и дорого).


## ЗАДАЧА 3: подзадачи бейджами
Над списком карточек - бейджи с номерами и темами подзадач из decompose, чтобы менеджер видел разложение заявки.