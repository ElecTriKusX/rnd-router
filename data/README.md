# Данные

Runtime-данные, например `profiles.json` и `index.parquet`, хранятся здесь или в объектном хранилище. Не коммитьте персональные данные и сгенерированные индексы без явного решения по их обработке.
